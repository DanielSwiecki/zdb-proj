spring:
  application:
    name: zdb-project
  datasource:
    url: jdbc:postgresql://localhost:12345/zdb
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  flyway:
    enabled: false
    locations: classpath:db/migration

server:
  port: 8080
